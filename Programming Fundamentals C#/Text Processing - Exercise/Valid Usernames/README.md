Create a program that reads usernames on a single line (joined by ", ") and prints all valid usernames. 
A valid username:

	has length between 3 and 16 characters
	contains only letters, numbers, hyphens and underscores

